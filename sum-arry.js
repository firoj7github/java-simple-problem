const numbers = [21, 32, 41, 23, 65];
let sum = 0;
for(let i=0 ; i<numbers.length ; i++){
    const element = numbers[i];
    sum = sum + element;
   
}

console.log(sum);


function arrayTotal (number){
    let sum = 0;
    for(i=0; i<number.length; i++){
        const elements = number[i];
        sum = sum + element;
        return sum ;
    }
}

console.log(sum);